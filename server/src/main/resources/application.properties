# server/src/main/resources/application.properties

server.port=8080

gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-pro:generateContent
gemini.api.key=${GEMINI_API_KEY} # Use environment variable for the API Key

# --- THIS IS THE FIX ---
# Instead of a classpath resource, we get the file path from the environment variable
# that Render automatically creates for our secret file.
gcp.credentials.path=${GCP_CREDENTIALS_JSON_PATH}
# --- END OF FIX ---